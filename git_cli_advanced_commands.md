
---

## 🛠️ GitHub/Git コマンドライン操作メモ

このメモは、TortoiseGitのGUI操作では対応が困難または非効率な、コマンドラインでの高度なGit操作（履歴のクリーンアップ、詳細なリモート管理など）を目的としています。

### 1. リモートリポジトリの管理

| 項目 | コマンド | 機能 |
| :--- | :--- | :--- |
| **リモート一覧確認** | `git remote -v` | リモートリポジトリの短縮名とURL（fetch/push）を一覧表示する。 |
| **リモートURL変更** | `git remote set-url <REMOTE_NAME> <NEW_URL>` | リモートの接続URLを更新する（例: HTTPSとSSHの切り替え）。 |
| **不要なリモート削除** | `git remote remove <REMOTE_NAME>` | 使用しなくなったリモート設定を削除する。 |

---

### 2. コミット履歴のクリーンアップと修正

| 項目 | コマンド | 機能 |
| :--- | :--- | :--- |
| **直前コミット修正** | `git commit --amend` | 直前のコミットメッセージや内容を修正し、上書きする（Push前のみ）。 |
| **ステージング解除** | `git restore --staged <FILE_NAME>` | `git add`でステージングしたファイルを、作業ディレクトリに残したまま解除する。 |
| **未追跡ファイル一括削除** | `git clean -fd` | Gitが管理していないファイルやディレクトリ（ビルド生成物など）を強制削除する。 |
| **履歴の統合 (Squash)** | `git rebase -i <COMMIT_HASH>` | 複数のコミットを対話形式で一つに統合し、履歴を簡略化する。 |

---

### 3. ブランチの高度な操作

| 項目 | コマンド | 機能 |
| :--- | :--- | :--- |
| **リモート追跡ブランチ削除** | `git push <REMOTE_NAME> --delete <BRANCH_NAME>` | GitHubなどのリモートリポジトリ上のブランチを削除する。 |
| **特定ブランチの取得** | `git fetch <REMOTE_NAME> <BRANCH_NAME>:<BRANCH_NAME>` | 指定したリモートブランチのみをローカルに取得する（チェックアウトはしない）。 |
| **コミットの取り込み (Cherry-pick)** | `git cherry-pick <COMMIT_HASH>` | 特定のコミット一つだけを、現在のブランチに適用する。 |

---

### 4. 外部ファイル (`requirements.txt`) の操作

| 項目 | コマンド | 機能 |
| :--- | :--- | :--- |
| **環境依存性出力** | `pip freeze > requirements.txt` | 現在の仮想環境のパッケージ一覧を依存関係ファイルとして出力する。 |
| **依存性の一括インストール** | `pip install -r requirements.txt` | 依存関係ファイルに基づき、パッケージを一括インストールする。 |

---

### 5. リポジトリの初期化

| 項目 | コマンド | 機能 |
| :--- | :--- | :--- |
| **新規リポジトリ作成** | `git init` | 新しいディレクトリをローカルのGitリポジトリとして初期化する。 |
| **リモート追加** | `git remote add origin <GITHUB_URL>` | ローカルリポジトリとGitHub上のリモートリポジトリを関連付ける。 |
